---
paths:
  - "**/*.wgsl"
---

Before changing WGSL, load `graphics:wgpu-core`, then
`graphics:wgsl-core`, then only the smallest routed spokes. Read the pinned
graphics conventions in `CODE.md`. Host layout, shader layout, coordinate
space, numeric output, and rendered pixels need separate evidence.

